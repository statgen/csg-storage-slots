#!/usr/bin/env perl

use FindBin qw($Bin);
use local::lib qq($Bin/../local);
use lib qq($Bin/../lib/perl5);

use CSG::Storage::Slots::Slot;

$ARGV[0] =~ s/^add-/add_/g;
CSG::Storage::Slots::Slot->run;
